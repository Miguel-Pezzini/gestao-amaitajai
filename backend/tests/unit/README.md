# Testes unitários

Coloque aqui testes que **não** dependem de PostgreSQL nem da API HTTP (validators, utilitários puros, funções isoladas).

Padrão de nome: `*.test.ts` (ex.: `agenda.utils.test.ts`).

Rodar só unitários (quando existirem):

```bash
npm run test:unit
```
